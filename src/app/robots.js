export default function robots() {
    return {
      rules: {
        userAgent: '*',
        allow: '/',
      },
      sitemap: 'https://vgb-profile.minimaledgetech.com/sitemap.xml', 
    };
  }